# STEP 1 - Basic TCP Chat

Python `socket`를 이용한 1:1 TCP 채팅 구현.

## 실행 방법

```bash
python server.py
python client.py   # 다른 터미널에서 실행
```

## 학습 내용
`socket()`, `bind()`, `listen()`, `accept()`, `connect()`, `send()`, `recv()`, `close()`
